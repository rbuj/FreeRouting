#Tue, 29 Mar 2016 16:21:32 +0200
annotation.processing.enabled=true
annotation.processing.enabled.in.editor=false
annotation.processing.processors.list=
annotation.processing.run.all.processors=true
annotation.processing.source.output=${build.generated.sources.dir}/ap-source-output
ant.customtasks.libs=JWSAntTasks
application.title=freeroute
application.vendor=robert
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form,**/.jar,help/**,po/**
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
build.generated.sources.dir=${build.dir}/generated-sources
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
# Uncomment to specify the preferred debugger connection transport:
#debug.transport=dt_socket
debug.classpath=${run.classpath}
debug.test.classpath=${run.test.classpath}
# Files in build.classes.dir which should be excluded from distribution jar
dist.archive.excludes=
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/freeroute.jar
dist.javadoc.dir=${dist.dir}/javadoc
endorsed.classpath=
excludes=ext/**,help/docbook*/**,po/**,help/po/**
file.reference.FreeRouting-src=src
includes=**
jar.archive.disabled=${jnlp.enabled}
jar.compress=false
jar.index=${jnlp.enabled}
javac.classpath=${ivy.classpath}\:${reference.JavaHelp.jar}\:${reference.JSearchIndexer.jar}
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false
javac.external.vm=true
javac.processorpath=${javac.classpath}
javac.source=1.8
javac.target=1.8
javac.test.classpath=${javac.classpath}\:${build.classes.dir}
javac.test.processorpath=${javac.test.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=${source.encoding}
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
jnlp.codebase.type=no.codebase
jnlp.descriptor=application
jnlp.enabled=false
jnlp.mixed.code=default
jnlp.offline-allowed=false
jnlp.signed=false
jnlp.signing=
jnlp.signing.alias=
jnlp.signing.keystore=
main.class=gui.MainApplication
# Optional override of default Application-Library-Allowable-Codebase attribute identifying the locations where your signed RIA is expected to be found.
manifest.custom.application.library.allowable.codebase=
# Optional override of default Caller-Allowable-Codebase attribute identifying the domains from which JavaScript code can make calls to your RIA without security prompts.
manifest.custom.caller.allowable.codebase=
# Optional override of default Codebase manifest attribute, use to prevent RIAs from being repurposed
manifest.custom.codebase=
# Optional override of default Permissions manifest attribute (supported values: sandbox, all-permissions)
manifest.custom.permissions=
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
mkdist.disabled=false
platform.active=default_platform
project.JavaHelp=../jh/javahelp/trunk/javahelp_nbproject
project.JSearchIndexer=../jh/javahelp/trunk/JSearchIndexer_nbproject
reference.JavaHelp.jar=${project.JavaHelp}/dist/lib/jh-client.jar
reference.JSearchIndexer.jar=${project.JSearchIndexer}/dist/jsearch-indexer.jar
run.classpath=${javac.classpath}\:${build.classes.dir}
# Space-separated list of JVM arguments used when running the project.
# You may also define separate properties like run-sys-prop.name=value instead of -Dname=value.
# To set system properties for unit tests define test-sys-prop.name=value:
run.jvmargs=
run.test.classpath=${javac.test.classpath}\:${build.test.classes.dir}
source.encoding=UTF-8
src.dir=${file.reference.FreeRouting-src}
languages.list=ca,de,en,es
ext.dir=${basedir}/ext
javahelp.src.dir=${ext.dir}/javahelp
javahelp.project.dir=${javahelp.src.dir}/trunk/javahelp_nbproject
javahelp.lib.file=${javahelp.project.dir}/dist/jhall.jar
docbook.base.dir=${basedir}/src/help
docbook.lib.dir=${docbook.base.dir}/lib
docbook.input.dir=${docbook.base.dir}/manual
docbook.output.dir=${basedir}/src/helpset
docbook.xsl.version=1.79.1
docbook.xsl.base.dir=${docbook.base.dir}
docbook.xsl.dir=${docbook.xsl.base.dir}/docbook
docbook.xsl.file=${docbook.xsl.base.dir}/docbook.zip
html.stylesheet=${docbook.xsl.dir}/html/docbook.xsl
javahelp.stylesheet=${docbook.xsl.dir}/javahelp/javahelp.xsl
docbook.xml.version=4.5
docbook.xml.base.dir=${docbook.base.dir}
docbook.xml.dir=${docbook.xml.base.dir}/docbook-xml-${docbook.xml.version}
docbook.xml.file=${docbook.xml.base.dir}/docbook.zip
xerces2-j.install.version=2.11.0
xerces2-j.jar.dir=${docbook.lib.dir}
xerces2-j.jar.file=${xerces2-j.jar.dir}/xerces2-j.jar
xalan2-j.install.version=2.7.2
xalan2-j.jar.dir=${docbook.lib.dir}
xalan2-j.jar.file=${xalan2-j.jar.dir}/xalan2-j.jar
saxon.install.version=6.5.3
saxon.jar.dir=${docbook.lib.dir}
saxon.jar.file=${saxon.jar.dir}/saxon.jar
ant-contrib.home=${user.home}/.ant
ant-contrib.jar.dir=${ant-contrib.home}/lib
ant-contrib.jar.file=${ant-contrib.jar.dir}/ant-contrib.jar
ivy.install.version=2.4.0
ivy.home=${user.home}/.ant
ivy.jar.dir=${ivy.home}/lib
ivy.jar.file=${ivy.jar.dir}/ivy.jar
ivy.lib.dir=${basedir}/lib

ivy.classpath=lib/junit-4.8.2-javadoc.jar\:lib/junit-4.8.2-sources.jar\:lib/junit-4.8.2.jar
